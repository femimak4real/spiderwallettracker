Flask==3.0.3
gunicorn==22.0.0
python-telegram-bot==22.0
urllib3==1.26.18
requests==2.32.3
Pillow==10.4.0
# cachetools and APScheduler are intentionally NOT pinned here.
# python-telegram-bot==13.15 hard-pins cachetools==4.2.2 and APScheduler==3.6.3
# in its own setup.py — pinning a different version ourselves just creates a
# ResolutionImpossible conflict pip can't resolve. Our code only uses
# TTLCache/LRUCache from cachetools and BackgroundScheduler/CronTrigger from
# APScheduler, both of which exist fine in those older versions, so we let
# pip install whatever PTB demands instead of fighting it.
